#include <iostream>

/* this is the first program of the first chapter,
 * so far so good I've learned how to make multiline
 * comments and now I will try to keep working hard
 * so that I can quickly learn C++ (btw I like this tutorial)!
 */

// this program prints a sentence
int main() {
    std::cout << "Hello World!\n"; // here we use the standard library cout to print a statement inserted by <<
    return 0;                      // returning 0 signals that the program has been succesfully executed
}
